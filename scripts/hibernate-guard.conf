# /etc/conf.d/hibernate-guard — Configuration for hibernate-guard service
# Swap usage threshold (percent) — hibernate disabled above this
SWAP_THRESHOLD=85

# Root filesystem usage threshold (percent) — hibernate disabled above this
ROOT_THRESHOLD=90
